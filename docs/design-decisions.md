# Design Decisions Log

> チームでの検討・メンタリングを通じて決めた「何を・なぜ」の記録。
> 迷ったらここに戻る。決定は変わってよいが、**変えた理由を必ず残す**。

## この製品は何か（進化後のビジョン）

当初は「SAFEWATCH＝警告を出すカスタム飛行モード」だったが、検討の結果、
**製品の主役は"非エンジニアでも簡単にドローンの巡回飛行を計画・実行できること"**だと再定義した。

> **一言でいうと**: 住所を入れるだけで、非エンジニアがその周辺を安全に巡回飛行できるツール。

警告機能（旧SAFEWATCH）は「主役」ではなく、
**素人が作った飛行計画を機体側が見守る"安全機構"**として位置づけ直した。

## 全体像：3階建てモデル

| 層 | 役割 | 誰向け | 主な技術 |
|---|---|---|---|
| ① 計画（Plan） | 「どこを・どう飛ぶか」を簡単に指定 | 非エンジニアが触る | 住所→座標変換(ジオコーディング)、Python |
| ② 実行（Execute） | 計画をミッション化して機体に送り飛ばす | 自動（裏方） | pymavlink / dronekit / MAVLink / AUTOモード |
| ③ 守る（Protect） | 飛行中の異常を監視して安全に飛ばす | 自動（裏方） | ArduPilot フライトコード（＝コース3の評価対象の芯） |

- **魅せ場・情熱の対象** = ①（Mission Plannerを使わない簡単な計画体験）
- **課題としての芯・評価される所** = ③（フライトコード側の安全機構）
- ①だけでも③だけでもなく、両輪でひとつの物語にする。

## 決定ログ

| # | 決めたこと | 検討した選択肢 | 採用 | なぜ | 状態 |
|---|---|---|---|---|---|
| D1 | デモの対象ユースケース | A巡回 / B塗りつぶし / C地点巡り / D | **A（外周巡回）のみ** | 1つを最後まで通す方がデモ・課題として強い。B〜Dは将来 | 確定 |
| D2 | 対象エリアの指定方法 | スクショをAIで座標化 / 住所→座標 / 中心＋サイズ直書き | **住所→座標（ジオコーディング）** | スクショは座標情報を持たず精度が出ない＝安全リスク。住所変換は枯れた技術で正確・無料(Nominatim等) | 確定 |
| D3 | スクショAI座標推定 | 採用 / 不採用 | **不採用** | ピクセルから正確な緯度経度は出せず、数十m誤差＝民家/送電線への誤飛行リスク | 確定（ボツ） |
| D4 | デモの計画レベル | Lv0登録済/Lv1住所→自動巡回/Lv2地図なぞり | **Lv1（住所→既定サイズで自動巡回）** | 既存の四角ミッション生成コード＋ジオコーディングで、映える×作れる | 確定 |
| D5 | ルートの細かいカスタム | デモに入れる / 後回し | **後回し（ロードマップ）** | まず「住所→飛ぶ」を通す。カスタムは"次にやること"として発表で語る | 確定 |
| D6 | AIの使いどころ | 座標推定に使う / 自然言語の意図理解に使う | **自然言語の意図理解（将来）** | AIは言葉の理解が得意・座標の発明は苦手。役割を正しく割り当てる | 方針（将来） |
| D7 | ③安全機構の芯を残すか | 残す / ①だけにする | **残す** | コース3は「フライトコード編」。①だけだとフライトコード理解の証拠が薄い | 確定 |
| D8 | 地図でのルート選択方法 | 箱ドラッグ / 角クリック多角形 / クリック+ドラッグ修正 | **角クリックで多角形（🅑）** | 実際の畑の形をなぞれる。②実行層をそのまま流用できる | 確定（Lv1の次のマイルストーン） |
| D9 | 実装の順番 | 地図から作る / 配管から作る | **Lv1(住所→自動巡回)で配管を先に通し、次に🅑地図を載せる** | 配管全体が動くことを先に証明。入力だけ差し替える形で🅑が乗る | 確定 |
| D10 | チームの製品テーマ | — | **①安全機能の全統合 ②非エンジニア向けUI/UX ③既存の安全なフライトモードの再構築・拡張** | 先行事例調査(下記)で新機能の余地は小さいが「簡単さ＋統合」は空席。自作・拡張はチームの学習(コース3の目的)に直結 | 確定（方向） |
| D11 | 先行事例を踏まえた差別化 | 新機能を作る / 簡単さと統合で差別化 | **簡単さ＋統合で差別化** | 多角形→ミッション/フェンス/pre-armチェックは既存(QGC/MP/Lua)。空席は"非エンジニアのわかりやすさ" | 確定 |
| D12 | ③拡張の難易度 | Lv1最小追加 / Lv2振る舞い変更 / Lv3新規モード自作 | **Lv1〜Lv2（最小拡張）** | ゼロから新規自律モードは初心者チームには重く未完リスク。「既存に1個足して深く理解」で十分。※要件のためでなく"完成"のための線引き | 確定 |
| D13 | 評価要件の前提 | 明確な卒業要件あり / 自由（自主性重視） | **自由：明確な要件なし、各自が作りたいものを掘り下げる** | 講師からの情報。判断軸を「外部要件の充足」→「チームが学びたいこと＋完成可能性」へ切替。各自が3階建ての別の階を深掘りする形にできる | 確定 |
| D14 | チーム共有デモの作り方 | 全部作り込む / 薄く一本通す | **薄く一本通す（walking skeleton）** | 6ステップ全部を"繋がって動く"最小構成に。各ピースは最小(安全は親切版=Python監視、地図/C++は後)。背骨(座標→四角→SITL AUTO)から組む | 確定 |
| D15 | リーダー/UX担当 | — | **本人＝全体統括＋①UI/UX** | 会話を通じ製品方向を主導してきた適性。②配管・③フライトコードは他メンバーが深掘り | 確定 |
| D16 | 製品の形態 | スマホアプリ / ブラウザWebアプリ | **ブラウザWebアプリ** | ブラウザでルート指定→裏側でArduPilot(SITL/実機)にMAVLinkで接続。PCで地図を大きく使える | 確定 |
| D17 | ルート作成画面 | 無し / 追加 | **追加（今は四角・大きさ/高さ調整可）** | 「場所確認」と別に、ルートを作る画面を設ける。将来は多角形なぞり(D8)へ発展 | 確定 |
| D18 | 説明での③の位置づけ | 既存の統合のみ / フライトコードも最適化 | **フライトコードも最適化・拡張すると伝える** | チーム説明で③の主体性を明示。ArduPilot側の改善もテーマに含める | 確定 |
| D19 | UIの入口 | 単一ウィザード / 管理画面(ダッシュボード) | **ダッシュボード入口＋複数シナリオ管理** | 「色々なシチュエーション」に対応。登録済みみまわりを一覧・実行/新規作成。デモ説明もここから始める | 確定 |
| D20 | 製品名（仮） | — | **「みまわり自作ドローン」（仮）** | 親しみやすく用途が伝わる。正式名はチームで再検討可 | 仮 |
| D21 | ③安全機構の実装形 | (a)常時監視FS / (b)独自モード / (c)併走監視 | **(a)フェイルセーフ+フェンス（設定）＋(c)Lua監視のハイブリッド** | 実ソース確認：モードは常に1つで「独自モードで監視」は不可。ArduPilotは"監視→危険時に`set_mode()`でモード切替"の設計。AUTO巡回＋異常時RTL/LAND/BRAKEが本来形。詳細は [safety-design.md](safety-design.md) | 確定 |
| D22 | ③の実装言語 | Lua / C++ | **まずLua、深掘り箇所が定まったらC++** | 再コンパイル不要で反復が速く、安全機構の全体像を先に掴める。SITLでそのまま試せる。C++は`AP_Arming_Copter`や各モードへ後で降りる | 確定 |
| D23 | ドキュメントの公開/非公開 | 全部公開 / 全部非公開 / 分割 | **分割運用** | 本リポは公開の提出用。設計・検証・プロセスの証跡は公開に残し（評価の材料）、**個人名・日程・議事録は private リポ**([mimawari-team-notes](https://github.com/kenjiked/mimawari-team-notes))へ。push済みの内容は履歴に残るため、PIIは最初から公開側に書かない | 確定 |
| D24 | フライトコード(C++)比率の引き上げ | アプリ中心のまま / C++を段階的に増やす | **段階的に増やす（Lv1 pre-arm → Lv2 Lua監視のC++移植 → Lv3 自作モードMIMAWARI）** | 製品設計としてアプリ寄りは正しかった（D21で既存FS＋Luaに乗るのが本来形と確認）が、コース3「フライトコード編」の学びの証拠としてはC++が薄い。**再発明ではなく"学びのための下層移植"**: Luaで素振りした機能を自分の手でファーム本体へ降ろす。各Lvは独立にデモ可能＝時間切れでも成果が残る。旧SAFEWATCH（独自モードで監視）は不可だが、Lv3は独自モードで**飛行挙動を変える**ので設計的に正当。詳細は safety-design.md §5 Step3 | 方針（MTGで確定） |
| D25 | ダイバート（避難地点）フェイルセーフ | 常にホームへRTL / 避難地点へ転換可能に | **避難地点対応（本家Rally機能に乗る＋賢い選択を自作）** | 航空の「代替飛行場」の発想。実ソース確認: `AP_Rally` が存在し、Copter RTLは既に**最寄りラリーポイントorホーム**を帰還先に選ぶ（`mode_rtl.cpp:471`）＝土台は本家にある。再発明せず、①UIで避難場所を地図タップ登録（非エンジニア化＝主戦場）→②Luaで**エネルギー認識ダイバート**（残電池でホームに届かない時だけ避難地点へ。本家の"最寄り"を超える付加価値）→③C++移植（D24 Lv2教材）。詳細は safety-design.md §9 | 方針（MTGで確定） |
| D26 | 製品版の開発形態 | モックを拡張 / 別リポで製品版を新規開発 | **別リポ safe-route-planner（Companion Computer上のC++20サービス）として新規開発** | モック（本リポのWebアプリ）で検証した判断ロジック（帰還可能性・退避優先順位・河川LAND禁止・UNKNOWN安全側・全コマンド既定ブロック）を製品仕様書に基づきC++へ移植。ArduPilot本体は改造せず上位判断レイヤーとして実装（標準FSを置換しない）。M1完了: モデル/Preflight20チェック/ReturnHomeEstimator/DivertPlanner/6段コマンドゲート/GoogleTest34件全パス。3段階RuntimeMode(OBSERVER既定)・feature flag全false・dry_run=true の Safety by default | 進行中（M1完了） |

| 層 | デモに入れる | 後回し（ロードマップ） |
|---|---|---|
| ①計画 | 住所 → 周辺を**既定サイズで自動巡回**ルート生成 | ルート数値カスタム / 地図なぞり / AI自然言語 |
| ②実行 | ミッション生成→アップロード→AUTOで**SITL飛行** | 実機飛行 |
| ③守る | 飛行中の**安全監視**（姿勢・高度・アーム状態など） | 異常時の自動アクション |

**デモの一言**: 「住所を入れるだけで、非エンジニアがその周辺を安全に巡回飛行できる」をSITLで実演。

## 未解決・要確認（次の対話で詰める）

- ~~**③安全機構の実装形が未確定**~~ **解決(D21/D22)**:
  実ソースを読んで確定。**(a)フェイルセーフ+ジオフェンス（設定）＋(c)Lua監視のハイブリッド**、言語は**まずLua**。
  「独自モードで監視」は不可（モードは常に1つ）＝ArduPilotは"監視→危険時に`set_mode()`でモード切替"の設計だと判明。
  異常→発動フライトモードの対応表・pre-arm・実装ロードマップは **[safety-design.md](safety-design.md)** に集約。
- 住所ジオコーディングの提供元（無料のNominatim想定）と利用条件
- SITLのホーム座標を、ジオコーディング結果に合わせて動かす方法
- 既定の巡回サイズ・高度・周回数の初期値
- 提出形式（自分のrepo URL共有 / 指定repoへのPR）＝講師確認
- 本家ArduPilotへのPRは不要という理解でよいか＝講師確認
- ~~【評価基準】~~ **解決(D13)**: 明確な卒業要件は無く、各自が作りたいものを掘り下げる自由形式。
  → スコープの歯止めが外部に無いぶん、**完成可能性の自己管理**が最重要になった。
- ~~**LuaかC++か**~~ **解決(D22)**: まずLua、深掘り箇所が定まったらC++。
- ~~**③拡張の具体対象**~~ **解決(safety-design.md)**: 実コードを確認し、フェンス/バッテリ/GCS/EKFフェイルセーフ＋Lua pre-arm(aux-auth)＋Lua飛行中監視に確定。実装はStep1(設定)→Step2(Lua)→Step3(C++)の順。

## 先行事例調査（Prior Art / 2026-07 時点）

構想した部品は、既存ツールでほぼ全て実現済みと確認した。
→ **新機能ではなく「簡単さ＋統合」で差別化する**根拠。車輪の再発明を避け、巨人の肩に乗る。

| 構想部品 | 既存？ | どこに |
|---|---|---|
| 多角形を描く→巡回ミッション | ✅ ある | QGC「Survey」、Mission Planner「Polygon→Auto WP」「WP Circle(外周巡回)」 |
| ジオフェンス（範囲外で自動帰還） | ✅ ある | ArduPilot標準 |
| 計画を検査して離陸を拒否 | ✅ 枠組みあり | Pre-armチェック群＋公式 `arming-checks.lua`（警告/arming拒否を選べる） |
| 飛行中の姿勢監視 | ✅ ある | 姿勢系フェイルセーフ＋Luaテレメトリ監視 |
| 外周巡回そのもの | ✅ ある | fence-follow / dwell点 / WP Circle |

**空いている席**＝「非エンジニアが迷わないわかりやすさ」。既存ツールは高機能だが初心者には複雑。ここが我々の主戦場。

出典:
- QGC Survey: https://docs.qgroundcontrol.com/master/en/qgc-user-guide/plan_view/pattern_survey.html
- Mission Planner Overview: https://ardupilot.org/planner/docs/mission-planner-overview.html
- Pre-Arm Safety Checks: https://ardupilot.org/copter/docs/common-prearm-safety-checks.html
- arming-checks.lua: https://github.com/ArduPilot/ardupilot/blob/master/libraries/AP_Scripting/applets/arming-checks.lua
- Lua prearm auth: https://discuss.ardupilot.org/t/lua-prearm-auth-scripts/117827

## この決定が既存ドキュメントに与える影響

- `concept-demo.md` / `requirements-draft.md` は本方針に合わせて更新済み。
- ~~`role-assignment-draft.md` / `presentation-outline.md` / `speaker-notes.md` / `demo-script.md` は
  次の段階（③の形が決まった後）で、この新方針に合わせて見直す。~~
  **完了（2026-07-04）**: 役割分担・発表アウトライン・発表原稿を現行製品（3階建て・実データ安全チェック・
  安全機構の実飛行統合）に全面改訂。チームの進行方法は **[team-process.md](team-process.md)** に新設。
  `demo-script.md` は 7/1 版の台本に現状補記を追加（最新の一気通貫デモ手順は speaker-notes.md 側）。
